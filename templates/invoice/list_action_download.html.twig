{% if admin.hasAccess('edit', object) and admin.hasRoute('edit') and admin.hasRoute('download') and object.code is not null %}
    <a href="{{ admin.generateObjectUrl('download', object) }}" class="btn btn-sm btn-default" title="Download">
        <i class="glyphicon glyphicon-download"></i>
        Download
    </a>
{% endif %}
